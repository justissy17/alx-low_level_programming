#include "main.h"

/**
 * print_alphabet - entry point (make the alphabet)
 * Return: 0(successful)
 */

void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
				}
				_putchar('\n');
				}
